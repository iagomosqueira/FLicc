% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/flicc_plots.R
\name{plot_len}
\alias{plot_len}
\title{Plot observed and predicted length compositions}
\usage{
plot_len(
  fit,
  year = NULL,
  gear = NULL,
  by_gear = FALSE,
  title = NULL,
  observed_fill = "grey75",
  observed_colour = "black",
  fitted_colour = "blue",
  fitted_linewidth = 1,
  len_by = 10
)
}
\arguments{
\item{fit}{A fitted \code{flicc_tmb_fit} object.}

\item{year}{Optional year selection. Can be \code{NULL} for all years, a vector
of year values matching the year dimnames, or numeric indices.}

\item{gear}{Optional gear selection. Can be \code{NULL} for all gears, a vector
of gear names, or numeric indices.}

\item{by_gear}{Logical. If \code{TRUE}, panels are shown by year and gear. If
\code{FALSE}, selected gears are combined using \code{fit$tmb_data$catch_wt}.}

\item{title}{Optional plot title.}

\item{observed_fill}{Fill colour for observed length composition bars.}

\item{observed_colour}{Border colour for observed length composition bars.}

\item{fitted_colour}{Colour for fitted length composition lines.}

\item{fitted_linewidth}{Line width for fitted length composition lines.}

\item{len_by}{Integer giving the spacing of x-axis tick marks for length.}
}
\value{
A \code{ggplot} object.
}
\description{
Plots observed and predicted length compositions from a fitted
\code{flicc_tmb_fit} object using \code{ggplot2}. Observed length frequencies are
taken from \code{fit$report$obslen} and predicted length compositions from
\code{fit$report$predlen_gear}.
}
\details{
The function can subset by year and gear, and can either display separate
year-by-gear panels or aggregate across selected gears using the gear
weights in \code{fit$tmb_data$catch_wt}.

Observed length frequencies are standardized within each year and gear to
proportions before plotting. When \code{by_gear = FALSE}, the selected gears are
combined using the relative gear weights stored in \code{fit$tmb_data$catch_wt}.

The function expects the fitted object to contain:
\describe{
\item{\code{fit$report$obslen}}{An \code{FLQuants} object of observed length
frequencies by gear.}
\item{\code{fit$report$predlen_gear}}{An \code{FLQuants} object of predicted length
compositions by gear.}
}
}
\examples{
\dontrun{
fit <- fiticc(lfd_alfonsino,stklen_alfonsino,sel_fun=c("dsnormal","logistic"),catch_by_gear = c(0.7,0.3))
plot_len(fit)
plot_len(fit, year = 2022:2024)
plot_len(fit, gear = "Gillnet", by_gear = TRUE)
plot_len(fit, gear = c("Trawl", "Gillnet"), year = 2022:2024, by_gear = TRUE)
}

}
\seealso{
\code{\link[=plot_spr]{plot_spr()}}
}
