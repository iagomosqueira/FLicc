% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/flicc_mse.R
\name{hcr_sprlbi}
\alias{hcr_sprlbi}
\title{Combined SPR and index trend harvest control rule}
\usage{
hcr_sprlbi(
  fit,
  gear,
  thresh = 0.75,
  b1 = 0.1,
  b2 = 0.35,
  b3 = 0.5,
  b4 = 0.75,
  dlow = -0.2,
  dopt = 0,
  dhi = 0.15,
  nyrs = 1,
  n1 = 2,
  n2 = 3
)
}
\arguments{
\item{fit}{A fitted FLicc object.}

\item{gear}{Numeric or character index selecting the gear-specific indicator
from \code{LBIspr(fit, thresh = thresh)}.}

\item{thresh}{Threshold passed to \code{LBIspr()}.}

\item{b1}{Lower SPR trigger point.}

\item{b2}{SPR trigger where the no-change plateau starts.}

\item{b3}{SPR trigger where the upper slope starts.}

\item{b4}{SPR trigger where the maximum increase is reached.}

\item{dlow}{Relative change below \code{b1}, e.g. \code{-0.20} for a
20\\% reduction.}

\item{dopt}{Relative change between \code{b2} and \code{b3}, usually
\code{0}.}

\item{dhi}{Relative change above \code{b4}, e.g. \code{0.15} for a
15\\% increase.}

\item{nyrs}{Number of most recent years of \code{fit$report$spr} used to
calculate mean current SPR. Default is 1.}

\item{n1}{Number of recent years used by \code{r_rule()} for the numerator
mean. Default is 2.}

\item{n2}{Number of preceding years used by \code{r_rule()} for the
denominator mean. Default is 3.}
}
\value{
A numeric multiplier. Values below 1 imply a reduction, 1 implies
no change, and values above 1 imply an increase.
}
\description{
Apply a combined harvest control rule using current SPR and a recent
biomass-ratio trend indicator.
}
\details{
The rule first calculates a gear-specific biomass trend ratio using
\code{r_rule()} applied to \code{LBIspr(fit, thresh = thresh)[[gear]]}.
It then calculates an SPR-based change multiplier using \code{spr_rule()}.

The SPR multiplier is applied only when the biomass trend and SPR rule point
in the same direction:
\itemize{
\item if \code{r < 1} and \code{s < 1}, apply \code{s}
\item if \code{r > 1} and \code{s > 1}, apply \code{s}
\item otherwise, return \code{1}, meaning no change
}
}
\examples{
# Default combined SPR-ratio rule
# hcr_sprr(fit, gear = 1)

# Custom SPR control points
# hcr_sprlbi(
#   fit, gear = 1,
#   b1 = 0.10, b2 = 0.35, b3 = 0.50, b4 = 0.75,
#   dlow = -0.20, dopt = 0, dhi = 0.15
# )

# Use a three-year mean SPR and alternative trend windows
# hcr_sprr(fit, gear = 1, nyrs = 3, n1 = 3, n2 = 5)

}
