% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{as_FLQuants}
\alias{as_FLQuants}
\title{Convert FLicc TMB report output to FLQuant/FLQuants objects}
\usage{
as_FLQuants(fit, stklen)
}
\arguments{
\item{report}{A list of reported model outputs, typically returned by
\code{obj$report()} from a fitted TMB model. Expected elements include
\code{N_y}, \code{Sel}, \code{plen}, \code{plen_all_y}, \code{Fk}, and \code{spr_y}.}

\item{tmb_data}{A TMB data list used to fit the model. Expected elements
include \code{year_names}, \code{gear_names}, \code{LLB}, and \code{obs}.}
}
\value{
A named list containing \code{FLQuant} and \code{FLQuants} objects.
}
\description{
Converts selected elements from a fitted FLicc TMB report and its
associated \code{tmb_data} input list into a structured list of FLR objects.
The returned list contains \code{FLQuant} and \code{FLQuants} objects for
abundance, observed and predicted length compositions, selectivity,
fishing mortality by gear, and spawning potential ratio.
}
\details{
This helper is intended to provide a more FLR-friendly representation of
model output than the raw arrays returned by \code{obj$report()}.

The returned object is a named list with the following components:
\describe{
\item{\code{N}}{An \code{FLQuant} containing estimated numbers-at-length by year.}
\item{\code{sel}}{An \code{FLQuants} object with one \code{FLQuant} per gear,
containing selectivity-at-length.}
\item{\code{obslen}}{An \code{FLQuants} object with one \code{FLQuant} per gear,
containing observed length frequencies by year.}
\item{\code{predlen_gear}}{An \code{FLQuants} object with one \code{FLQuant} per gear,
containing predicted length compositions by year and gear.}
\item{\code{predlen}}{An \code{FLQuants} object containing predicted length
compositions by year for each gear-specific component returned in
\code{report$plen_all_y}.}
\item{\code{Fk}}{An \code{FLQuants} object with one \code{FLQuant} per gear,
containing fishing mortality by year.}
\item{\code{spr}}{An \code{FLQuant} containing spawning potential ratio by year.}
}

Length classes are mapped to the \code{quant}/\code{len} dimension, years to the
\code{year} dimension, and gears are represented as separate elements in
\code{FLQuants} objects.
}
\examples{
\dontrun{
rep <- fit$obj$report()
flqs <- as_FLQuants(rep, fit$tmb_data)

flqs$N
flqs$obslen
flqs$predlen_gear
flqs$predlen
flqs$spr
names(flqs$obslen)
names(flqs$predlen_gear)
}

}
\seealso{
\code{\link[FLCore:FLQuant]{FLCore::FLQuant()}}, \code{\link[FLCore:FLQuants]{FLCore::FLQuants()}}
}
